export type TMessage = {
  userId: string
  channelId: string
  message: string
  createdAt: string
  id: string
}

export type TPostMessage = {
  userId: string
  channelId: string
  message: string
}
